<%= form_for :relationships, url: user_relationship_path(user_id:
  current_user.id, id: @relationship.id), html: {method: :delete},
  remote: true do |f| %>
  <%= f.submit t(".unfollow"), class: "btn btn-danger" %>
<% end %>
